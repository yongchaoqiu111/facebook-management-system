# 清理各页面重复导航代码

## 任务清单

### 1. Home.vue
- [x] 删除重复的 `<header class="header">` HTML
- [ ] 删除 `.bottom-nav`, `.nav-item` 等 CSS（已部分删除）
- [ ] 保留 `navigate()` 函数（用于页面内跳转）

### 2. Contacts.vue  
- [ ] 删除重复的 `<header class="header">` HTML
- [ ] 删除重复的 `<footer class="bottom-nav">` HTML
- [ ] 删除 `.bottom-nav`, `.nav-item` 等 CSS

### 3. Wallet.vue
- [ ] 删除重复的 `<header class="header">` HTML
- [ ] 删除重复的 `<footer class="bottom-nav">` HTML
- [ ] 删除 `.bottom-nav`, `.nav-item` 等 CSS

### 4. Profile.vue
- [ ] 删除重复的 `<header class="header">` HTML
- [ ] 删除重复的 `<footer class="bottom-nav">` HTML
- [ ] 删除 `.bottom-nav`, `.nav-item` 等 CSS

### 5. Market.vue
- [ ] 删除重复的 `<header class="header">` HTML（已添加 page-container）
- [ ] 删除 `.bottom-nav`, `.nav-item` 等 CSS

---

## 注意事项

1. **保留 `navigate()` 函数** - 部分页面内跳转仍需要使用
2. **检查 header 内自定义按钮** - 如 Contacts 的 "+" 按钮，需要移到 AppHeader 的 slot 中
3. **iOS 适配已全局处理** - 删除页面内的 padding/margin 适配代码
4. **测试各页面** - 确保导航切换正常

---

## 优先级

- **高**：Contacts.vue, Wallet.vue, Profile.vue（完整重复）
- **中**：Market.vue（部分重复）
- **低**：Home.vue（已删除部分）

---

**预计工时**：2-3 小时
