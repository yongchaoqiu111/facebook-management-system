请建立并严格执行一套基于“虚拟员工”架构的分布式任务分配与开发工作流。具体配置、规范及执行流程如下：

1. **团队结构与目录映射**：
   - **前端团队（3人）**：分别对应目录 ​D:\weibo\usddt\1\​、​D:\weibo\usddt\2\​、​D:\weibo\usddt\3\​。
   - **后端团队（2人）**：分别对应目录 ​D:\weibo\usdchou\1\​、​D:\weibo\usdchou\2\​。
   - **指令载体**：每个员工目录下必须维护一个名为 `task.md` 的文件，作为该虚拟员工的专属任务指令板与进度记录表。

2. **核心开发规范（需同步给所有虚拟员工）**：
   - **通用标准**：所有员工需严格遵循项目现有的 API 接口文档（参考 ​API_接口文档.md​）与技术标准。
   - **前端规范**：
     - 技术栈：Vue 3 + Vite + Pinia + Socket.IO。
     - 架构原则：坚持“少监听器、统一数据源”。优先使用 Pinia 进行全局状态管理，严禁在组件间使用复杂的 Event Bus 或冗余的 Watcher，确保数据流向清晰。
   - **后端规范**：
     - 技术栈：Express + Socket.IO + MongoDB + Redis。
     - 架构原则：重点保障系统安全性（如严格的 JWT 校验、输入参数过滤）与高并发处理能力（如利用 Redis 缓存热点数据、使用异步队列处理耗时任务）。

3. **工作流执行步骤**：
   - **第一步：分析与拆解**。接收我的需求后，首先分析技术难点与依赖关系，将其拆解为具体的前端（UI/交互/状态）或后端（接口/数据库/逻辑）子任务。
   - **第二步：备份提醒与分配**。在指派任务前，**必须**明确提醒对应的虚拟员工：“在执行任何修改前，请先对相关文件或目录进行备份”。随后根据任务属性与复杂度，指派给最合适的虚拟员工。
   - **第三步：写入指令**。将详细的执行步骤、关键代码片段、调试要求及规范约束写入对应员工目录下的 `task.md` 文件中。内容应包含：任务目标、涉及文件、具体修改点、备份检查项。
   - **第四步：反馈待命**。指令写入完成后，仅回复“任务已分配”，等待我手动触发后续的执行或检查动作，不要自动开始编码。

请确认你已完全理解此工作流及开发规范，并准备好接收第一个任务。